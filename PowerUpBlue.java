import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class PowerUpBlue here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class PowerUpBlue extends Actor
{
    /**
     * Act - do whatever the PowerUpBlue wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        //if (getOneIntersectingObject(Ball.class) != null) {
        //getWorld().removeObject(this);
        //}
    }
}
